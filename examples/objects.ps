# Object/Map example
let person = {
    name: "Alice",
    age: 30,
    city: "New York"
}

# Access properties
print(person.name)
print(person.age)

# Get keys
let fields = keys(person)
print(fields)
